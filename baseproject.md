[Frontend: React/Vue + Apollo Client]
         ↕ GraphQL / REST
[Backend: Go + Gin + gqlgen]
    ├── Auth (JWT + 2FA)       ← обязательно по заданию!
    ├── Модуль взаимодействия с БД (GORM / SQLC)
    ├── Функциональный модуль (подбор/фильтрация серверов)
    └── Сетевой модуль (GraphQL endpoint + REST endpoints)
         ↕
[PostgreSQL]